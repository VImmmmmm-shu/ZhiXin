为舞蹈机器人项目添加新舞台场景
一、获取舞台资源
1. 下载来源
•	从淘宝购买 Unity 舞台资源包，格式为 .unitypackage。
2. 导入 Unity
•	双击 .unitypackage 文件或在 Unity 中通过 Assets → Import Package → Custom Package... 导入。
•	导入后资源出现在 Assets/TirgamesAssets/PBRStageEquipment/ 目录下。
二、集成舞台到主场景
1. 资源包结构
•	Scenes 文件夹：包含完整的舞台场景文件 PBRStageEqu。
•	Prefabs 文件夹：包含独立舞台部件（灯光、音响、DJ台、舞台底座等散装 Prefab）。
2. 合并舞台到主场景（多场景加载法）
•	在主场景中，右键点击 PBRStageEqu 场景文件 → Open Scene Additive（附加式加载）。
•	此时 Hierarchy 中同时显示主场景和舞台场景的物体。
•	将舞台场景根下的所有物体拖拽到主场景根下，使其成为主场景的一部分。
•	右键卸载已经空了的舞台场景根（Unload Scene）。
•	创建一个空物体 StageRoot，把所有舞台部件拖入其下，方便统一管理。
三、配置舞台
1. 调整位置和大小
•	选中 StageRoot，调整 Position（如 (0, 0, 0)）和 Scale（如 1 或根据场地大小调整）。
2. 重新烘焙导航网格（关键步骤）
•	选中所有舞台部件（特别是地板 StageBase），在 Inspector 右上角 Static 下拉中勾选 Navigation Static。
•	打开 Window → AI → Navigation，切换到 Bake 标签页，点击 Bake。
•	确认舞台表面出现浅蓝色导航网格，机器人才能走上去。
四、遇到的问题及解决方案
问题	原因	解决方案
导入报错 ColorPickerHDRConfig is obsolete	资源包基于旧版 Unity 开发，该 API 在 Unity 2022.3 及以上版本中已过时	找到 TirgamesStandardShaderGUI.cs 文件，注释掉第 92 行声明和在 DoEmissionArea 方法中的调用（去掉 m_ColorPickerHDRConfig 参数）
场景中有两个 Audio Listener 警告	场景中存在两个 Main Camera，每个 Camera 都自带一个 Audio Listener 组件	在 Hierarchy 中搜索 t:AudioListener，找到两个带有 Audio Listener 的物体（通常都叫 Main Camera），删除其中一个 Camera，或删除其中一个 Camera 上的 Audio Listener 组件
舞台自带背景音乐与舞蹈音乐冲突	舞台场景中某个物体挂载了 Audio Source 并设置了默认背景音乐	在 Hierarchy 中搜索 Audio Source，找到舞台自带的音乐源，禁用或删除该组件
运行时摄像机位置异常，看不到机器人正面	场景中存在两个 Main Camera，运行时激活了拍摄角度异常的摄像机	在 Hierarchy 中搜索 t:Camera，删除多余的 Camera，只保留一个 Main Camera；并确认剩余摄像机的 Position 和 Rotation 设置正确（建议 Position 设为 (0, 1, -5)，Rotation 设为 (0, 0, 0)）
或者不同的camera对应不同的场景，一个对应室外，一个对应室内，从而可以为不同的camera设置不同的坐标
五、最终效果
•	机器人站在专业舞台上，灯光、音响、幕布、桁架等一应俱全。
•	语音指令正常控制机器人在舞台上跳舞、换装、移动。
•	摄像机稳定跟随，无晃动。
•	舞蹈音乐正常切换，无舞台自带背景音乐干扰。
•	场景中只有一个 Audio Listener，无警告。
六、经验总结
1.	导入外部资源包时，优先检查编译错误和警告，先清理环境再继续搭建。
2.	多场景加载法比直接复制粘贴更安全，能避免遗漏物体和引用丢失。

