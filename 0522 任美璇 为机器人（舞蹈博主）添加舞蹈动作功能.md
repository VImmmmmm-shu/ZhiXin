为机器人（舞蹈博主）添加舞蹈动作功能
一、准备工作
1. 下载动画资源
•	访问 Mixamo 网站，选择人形角色，依次搜索并下载以下舞蹈动画文件：
o	Samba Dancing.fbx
o	Silly Dancing.fbx
o	House Dancing.fbx
o	Robot Hip Hop Dance.fbx
o	Rumba Dancing.fbx
o	Salsa Dancing.fbx
o	Twist Dance.fbx
•	下载设置：Format 选 FBX Binary(.fbx)，FPS 为 30，Skin 选 With Skin，Keyframe Reduction 选 none。
2. 导入 Unity
•	将七个 .fbx 文件拖入 Unity 项目的 Assets/Animations 文件夹。
•	逐一选中每个动画文件，在 Inspector 的 Rig 标签页中设置：
o	Animation Type：Humanoid
o	Avatar Definition：Create From This Model（因为它们是纯动画文件，无完整骨骼）
o	点击 Apply。
•	切换到 Animation 标签页，勾选 Loop Time（舞蹈需循环播放），点击 Apply。

二、Animator 控制器配置
1. 添加动画状态
•	双击打开角色使用的动画控制器 Ctrl_Dance_Sprinkler。
•	将七个动画文件全部拖入 Animator 窗口的 Base Layer 中，生成七个灰色状态块。
•	手动将它们分别重命名为：Samba、Silly、House、RobotHop、Rumba、Salsa、Twist，以方便管理。
2. 创建 Trigger 参数
•	在 Animator 窗口左上角切换到 Parameters 标签页。
•	点击 + → Trigger，分别创建：
o	Samba
o	Silly
o	House
o	RobotHop
o	Rumba
o	Salsa
o	Twist
3. 设置过渡
•	以桑巴舞为例：
o	右键 Any State → Make Transition，箭头拖到 Samba 状态。
o	点击过渡箭头，在 Inspector 中取消勾选 Has Exit Time，添加条件 Samba。
o	右键 Samba → Make Transition，箭头拖到 Movement（待机/行走混合树）。
o	点击过渡箭头，勾选 Has Exit Time，不添加条件（播完自动回到待机）。
•	对其他六种舞蹈重复同样的操作，确保每个舞蹈都有 Any State -> 舞蹈 和 舞蹈 -> Movement 的过渡。
三、语音脚本改造
1. 扩展关键词过滤
•	在 ContainsValidKeywords 方法的数组中添加舞蹈相关关键词：
o	"桑巴", "搞笑舞", "浩室", "机器人街舞", "伦巴", "萨尔萨", "扭扭舞", "跳舞", "舞蹈", "来段舞"
•	防止语音识别结果因不含原有关键词而被提前过滤。
2. 添加舞蹈指令处理
•	在 ProcessActionCommand 方法中，在其他动作判断之前加入随机舞蹈和具体舞蹈的识别逻辑：
o	随机舞蹈：“跳个舞”“跳舞”“来段舞”会从七种舞蹈中随机选一个播放。
o	具体舞蹈：根据用户指令中的关键词触发对应的 Trigger，并播放语音反馈。
•	代码片段示例：
csharp
// 随机舞蹈
if (userInput.Contains("跳个舞") || userInput.Contains("跳舞") || userInput.Contains("来段舞") || userInput.Contains("舞蹈"))
{
    string[] dances = { "Samba", "Silly", "House", "RobotHop", "Rumba", "Salsa", "Twist" };
    string pick = dances[UnityEngine.Random.Range(0, dances.Length)];
    animator.SetTrigger(pick);
    PlayTextToSpeech("好的，跳舞！");
    return true;
}
// 具体舞蹈
if (userInput.Contains("桑巴"))
{
    animator.SetTrigger("Samba");
    PlayTextToSpeech("来段桑巴");
    return true;
}
// ...（其余舞蹈同理）
3. 增强纠错能力
•	在 CorrectAndInferIntent 中添加舞蹈类误识别清洗，例如：
o	"丧巴" → "桑巴"、"伤疤" → "桑巴"
o	"好是" → "浩室"、"轮巴" → "伦巴"
o	"洒洒" → "萨尔萨"、"牛牛舞" → "扭扭舞"
•	同时保留原有的“简称在”“放下”等清洗逻辑。
4. 完整代码替换
•	将修改后的完整 SpeechToTextAndLLM.cs 脚本替换原有脚本，保存并等待编译完成。
四、测试与问题处理
1. 测试舞蹈触发
•	运行游戏，分别说出“桑巴”“伦巴”“跳个舞”等指令，机器人均能正确播放对应舞蹈动画。
•	随机舞蹈功能正常，每次说“跳个舞”会随机选择一种舞蹈。
2. 遇到的问题及解决
•	问题： 控制台出现 Release of invalid GC handle. The handle is from a previous domain. 警告。
•	分析： 该警告是由于脚本重新编译后，Unity 内部资源句柄未完全释放所致，不影响游戏运行。
•	处理： 完全重启 Unity 编辑器后警告消失，确认功能正常。该警告属于无害警告，可忽略。
3. 向后兼容验证
•	原有换装、行走、帽子、跳跃、攀爬、慢跑、右转、谢谢等功能均保持正常，新舞蹈指令未干扰原有逻辑。
五、工作总结
这次成功为机器人角色添加了七种舞蹈动作的语音控制功能，并保留原有各项功能不受影响。通过 Animator 状态机配置和语音脚本的扩展，实现了灵活的动作切换。针对语音识别的常见误读增加了纠错规则，提高了指令识别准确率。整个流程从资源获取、动画导入、状态机配置到代码集成，完整且可复用，为后续继续添加新动作奠定了基础。

